<select id="selectDeptSalary" resultType="map">
  SELECT D.DEPT_CD
       , D.DEPT_NM
       , COUNT(1)         AS EMP_CNT
       , SUM(S.PYMNT_AMT) AS TOTAL_AMT
    FROM TB_EMP D
    JOIN TB_SALARY S ON S.EMPNO = D.EMPNO
  <where>
    <if test="salyYm != null and salyYm != ''">
      AND S.SALY_YM = #{salyYm}
    </if>
    <if test="deptCd != null and deptCd != ''">
      AND D.DEPT_CD = #{deptCd}
    </if>
    AND D.USE_YN = 'Y'
  </where>
   GROUP BY D.DEPT_CD, D.DEPT_NM
   ORDER BY ${orderBy}
</select>
